/**
 * Previously rendered four L-shaped viewport corner brackets per DESIGN.md
 * §Persistent chrome. Removed at user request — the brackets read as visual
 * clutter rather than brand signal. Stub kept so `app/layout.tsx` doesn't
 * need a separate edit and so the component can be re-enabled later if the
 * decision changes.
 */
export function PixelCornerFrames() {
  return null
}
